#### 1、k8s集群环境报错fork: Cannot allocate memory

描述：测试环境遇到服务器操作命令提示fork:cannot allocate memory

解决：根据网上资料显示可能是进程数满了：
修改最大进程数后系统恢复

```
echo 1000000 > /proc/sys/kernel/pid_max
```

永久生效

```
echo "kernel.pid_max=1000000 " >> /etc/sysctl.conf
sysctl -p
```

